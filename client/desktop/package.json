{
  "name": "fe-server-desktop",
  "version": "1.0.0",
  "description": "desktop package",
  "engines": {
    "node": ">=8.9.0"
  },
  "main": "index.js",
  "dependencies": {
    "moment": "^2.19.3",
    "fego": "git+https://github.com/fegos/fego.git#release",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-router": "^4.1.1",
    "react-router-config": "^1.0.0-beta.3",
    "react-router-dom": "^4.1.1",
    "server-style-loader": "^1.0.0"
  },
  "scripts": {
    "init": "npm install --no-optional --no-package-lock --registry=https://registry.npm.taobao.org",
    "init-prod": "npm install --prefer-offline --production --no-optional --registry=https://registry.npm.taobao.org"
  }
}
